# Exhaustive and Greedy Search algorithms - AA
Find a minimum edge dominating set for a given undirected graph

## How to run:

```bash
$ pip install -r requirements.txt
$ python3 src/main.py <number of vertices>
```